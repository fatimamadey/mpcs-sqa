[pytest]
# Show short test summary for all outcomes
addopts = -v --tb=short

# Register the --browser command-line option (defined in conftest.py)
# so pytest doesn't warn about unknown options.
filterwarnings =
    ignore::DeprecationWarning

[tool:pytest]
log_cli = true
log_cli_level = INFO
