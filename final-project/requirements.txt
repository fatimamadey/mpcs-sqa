# ──────────────────────────────────────────────────────────────────────────────
# Django Poll App – Final Project dependencies
# ──────────────────────────────────────────────────────────────────────────────

# ── Django & core app deps ────────────────────────────────────────────────────
Django>=4.2,<5.0
Pillow>=10.0.0          # used by any ImageField / avatar upload

# ── Linting (Q1) ─────────────────────────────────────────────────────────────
flake8>=7.0.0
flake8-bugbear>=24.0.0  # opinionated bugbear warnings

# ── Testing – unit & integration (Q3, Q7) ────────────────────────────────────
pytest>=8.0.0
pytest-django>=4.8.0
pytest-cov>=5.0.0
pytest-html>=4.1.0

# ── Testing – UI automation with Playwright (Q5) ─────────────────────────────
playwright>=1.44.0
pytest-playwright>=0.5.0

# ── Performance testing – k6 is a standalone binary; no pip package needed ───
# Install k6 separately:  brew install k6
